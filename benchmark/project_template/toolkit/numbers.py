"""Numeric operations (unimplemented stubs)."""

from __future__ import annotations


def add(a: int, b: int) -> int:
    """Return the sum of ``a`` and ``b``.

    Examples:
        >>> add(2, 3)
        5
    """
    raise NotImplementedError


def factorial(n: int) -> int:
    """Return ``n!``. Raise ``ValueError`` if ``n`` is negative.

    Examples:
        >>> factorial(4)
        24
        >>> factorial(0)
        1
    """
    raise NotImplementedError


def is_prime(n: int) -> bool:
    """Return whether ``n`` is a prime number (integers < 2 are not prime).

    Examples:
        >>> is_prime(7)
        True
        >>> is_prime(1)
        False
    """
    raise NotImplementedError
