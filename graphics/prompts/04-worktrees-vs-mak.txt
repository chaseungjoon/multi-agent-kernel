Use case: infographic-diagram. Create a clear visual explanation comparing branch-and-merge coding with MAK's coordinated shared source. The attached image is a STYLE REFERENCE ONLY, not an edit target. Match the attached image's playful retro developer-manual pixel-art style very closely: midnight navy background, lavender, mint and peach accents, warm white readable monospace labels, tiny charming terminal tools, chunky pixel-art outlines and illustrated source-code objects. Compact illustrated explanation for a GitHub README, not branding or a business presentation. No large title, paragraph, numbered steps, card grid, footer, logo, slogan, glossy 3D, or neon effects. Opaque full-bleed wide image, around 1680x936.
Use two compact horizontal illustrated paths, one above the other, separated by empty space rather than a divider or card.
TOP PATH labeled "worktrees": start with a single source document at left; split arrows lead to THREE separate source documents across the middle, arranged vertically in a compact group, each document edited by its own little lavender/mint/peach terminal tool. These are whole separate copies; each document has a full set of code strokes. Label this group "separate copies". Three arrows from those copies converge into a mechanical funnel at the right labeled "merge", then exit to one assembled document at far right. A very small amber exclamation symbol beside the funnel suggests possible reconciliation; no red X, no failure claim.
BOTTOM PATH labeled "MAK": three small lavender/mint/peach terminal tools at left send three short distinct arrows through THREE matching closed padlocks labeled collectively "locks first". Arrows continue into ONE shared source storage frame containing three colored source-code strips, labeled "shared nodes". Then ONE arrow labeled "rebuild" leads to ONE assembled Python document at far right. This shared structure is visually quite different from the top row's three separate copies.
Use clear left-to-right arrowheads. Both rows end in a usable file. The point is where coordination happens, NOT that merging always fails or that MAK is always faster. ONLY labels: "worktrees", "separate copies", "merge", "MAK", "locks first", "shared nodes", "rebuild". Everything else explained visually. Have enough internal separation for instant readability at 850px wide. Keep the labels short and the illustrations substantial.
