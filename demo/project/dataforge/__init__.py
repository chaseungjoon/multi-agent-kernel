"""dataforge — a tiny library of pure data-wrangling helpers.

Every function ships as an unimplemented stub with a precise docstring; the test
suite under ``tests/`` is the full specification. The demo exercise is to let MAK
implement the stubs concurrently — see ``demo/README.md``.
"""
